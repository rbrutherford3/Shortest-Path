package shortestPath;

// Constants reflecting the different input states, or "modes"
// that can be changed with the radio buttons
public enum InputState {
	EDIT_NETWORK,
	PICK_START,
	PICK_FINISH
}
